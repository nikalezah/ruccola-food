ktor {
    storage {
        path = "files"
    }
    jwt {
        domain = "https://jwt-provider-domain/"
        audience = "jwt-audience"
        realm = "kz.ruccola.food"
        secret = "secret"
    }
}
