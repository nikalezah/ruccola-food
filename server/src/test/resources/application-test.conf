ktor {
    storage {
        path = "build/test-uploads"
    }
    jwt {
        domain = "https://jwt-provider-domain/"
        audience = "jwt-audience"
        realm = "kz.ruccola.food"
        secret = "secret"
    }
}
